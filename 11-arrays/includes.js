'use strict';

const movements = [200, 450, -400, 3000, -650, -130, 70, 1300];
console.log(movements);

// INCLUDES
// tests if an array contains a specified value
console.log(movements.includes(-130));
